# Template only — DO NOT commit a real key to Git.
# Create the real secret imperatively:
#   kubectl create secret generic groq-secret --from-literal=GROQ_API_KEY=gsk_xxx
apiVersion: v1
kind: Secret
metadata:
  name: groq-secret
type: Opaque
stringData:
  GROQ_API_KEY: ""          # empty -> app runs in Analytics Mode
